type SearchHistory {
  id: Int!
  date: DateTime
  query: String
}

type Query {
  searchHistory: [SearchHistory]
  instantSearch(query: String): [String]
}

type Mutation {
  addSearchHistory(query: String): SearchHistory
  removeSearchHistory(id: Int!): Boolean
}
