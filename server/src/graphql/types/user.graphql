type Address {
  zipcode: String!
  address: String!
  addressDetail: String
}

type User {
  name: String
  email: String
  phoneNumber: String
  address: Address
}

type Query {
  currentUser: User
}
